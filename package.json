{
  "name": "donation-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "src"
  ],
  "scripts": {
    "build": "chmod +x deploy.sh && ./deploy.sh",
    "start": "node --experimental-modules server.js"
  }
}
