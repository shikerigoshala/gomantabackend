{
  "name": "donation-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "git submodule update --init --recursive && cd server && npm install",
    "start": "cd server && npm start"
  }
}
