{
  "name": "donation-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "src"
  ],
  "scripts": {
    "build": "cd server && npm install",
    "start": "cd server && node --experimental-modules server.js"
  }
}
